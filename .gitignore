# OS
.DS_Store
Thumbs.db

# JetBrains IDE
.idea/
*.iml

# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log
# Keep the lock file committed for reproducible provider versions:
# .terraform.lock.hcl (do NOT ignore)

# Terraform variables (keep secrets out)
*.tfvars
*.tfvars.json

# Docker Compose runtime env (generated on the EC2 host)
.env
.env.*
compose/.env

# Local override compose files (developer-only)
docker-compose.override.yml
compose.override.yml

# PoweShell local scripts
recreate-with-smaller-type.ps1

# Logs
*.log
logs/
log/

# Editor/OS
*.swp
*.swo

# Keys and secrets
*.pem
*.key
*.pub
llm-deploy
llm-deploy.pub

# Optional frontend leftovers
node_modules/
dist/
build/

# Python virtual envs
.venv/
venv/